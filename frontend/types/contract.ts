export type ContractsDict = {
    nfts: {
        collectionId: string;
    };
    cw3FixedMultisig: {
        codeId: string;
        // address: string;
    };
    // icaAccount: {
    //     address: string;
    // };
    // icaController: {
    //     address: string;
    // };
    icaFactory: {
        address: string;
    };
    proxyMultisig: {
        address: string;
    };
};
