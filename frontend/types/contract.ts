export type ContractsDict = {
    cw3FixedMultisig: {
        codeId: number;
        // address: string;
    };
    // icaAccount: {
    //     address: string;
    // };
    // icaController: {
    //     address: string;
    // };
    nomosFactory: {
        address: string;
    };
    icaFactory: {
        address: string;
    };
};
